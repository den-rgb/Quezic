version: '3.8'

services:
  quezic-proxy:
    build: .
    container_name: quezic-youtube-proxy
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    # Uncomment below for resource limits
    # deploy:
    #   resources:
    #     limits:
    #       memory: 1G
    #     reservations:
    #       memory: 512M
